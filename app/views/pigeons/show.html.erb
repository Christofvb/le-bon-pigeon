<div class="container">
<div class="row">
  <div class="bp-index-container">
    <h1>Votre pigeon coup de coeur</h1>
    <div class="justify-content-center">
      <%= render @pigeon %>

      <div>
        <%= form_with(model: [@pigeon, @booking], local: true) do |form| %>
          <div class="field">
            <%= form.label :date_start %>
            <%= form.date_field :date_start, class: "datepicker" %>
          </div>

          <div class="field">
            <%= form.label :date_end %>
            <%= form.date_field :date_end, class: "datepicker mt-1" %>
          </div>

          <div class="actions">
            <%= form.submit "Réserver", class: "btn btn-primary mt-2 mb-2 pl-2" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
      
    
  </div>
    <%= link_to "Back to pigeons", pigeons_path, class: "btn btn-light mb-2 mt-2" %>
</div>